package veinteream;

import com.mysql.cj.x.protobuf.MysqlxDatatypes;

public class Question4 {

    // longest recurring substring
    // "abcabca" => abca
    // there will always be only one anaswe
    public String findSubstring(String text) {
        int maxSize = 1 + (text.length() / 2);
        Map<MysqlxDatatypes.Scalar.String,Integer>
        while (maxSize > 0){


        }

        return "";
    }


}
